Je veux maintenant un système de connection. Pour facilité ce système on ne pourra se connecter uniquement via notre compte google
Nous allons procédé si nécéssaire pas à pas dans l'implémentation de cette fonctionnalité.
L'idée et que à la première installation de notre application si nous ne sommes pas connécté avant il faut nous rediriger 
vers la page qui ressemble à la screen. à toi de faire cette page. L'image qui se trouve au milieu de la screen je l'ai il te suffit
de mettre l'emplacement de l'image (le lien) dans le code que je modifierait moi.
Ensuite quand on clique sur "se connecter via google" sa doit être comme sur toutes les autres applications une connection
via compte google basique  